---
phase: 04-auth
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/login/google-button.tsx
  - src/app/(auth)/login/page.jsx
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "Visiting /login renders the Pruma navy gradient with two blur blobs and a centered glassmorphism card containing the Google sign-in button"
    - "Clicking the Google button calls signIn('google', { callbackUrl: '/dashboard' }) and shows a Loader2 spinner with 'Conectando…' text while the OAuth round-trip is in flight"
    - "The card and its contents follow the UI-SPEC vertical hierarchy: brand (Barlow 28px) → h1 (Barlow 24px) → sub-headline → button → helper text"
    - "The legacy SPA wrapper src/app/(auth)/login/page.jsx is removed so Next.js resolves only the new .tsx route"
  artifacts:
    - path: "src/app/(auth)/layout.tsx"
      provides: "Server Component layout for the (auth) route group — navy gradient, two decorative blobs, centered slot for the card, absolute footer"
      contains: "from-[#0D1B4B]"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Server Component for /login — renders the glassmorphism card with brand, headings, GoogleButton client component, helper text"
      contains: "Bem-vindo de volta"
    - path: "src/app/(auth)/login/google-button.tsx"
      provides: "Client Component button calling signIn('google'); inline Google SVG (no library); 5 interaction states per UI-SPEC"
      contains: "signIn('google'"
  key_links:
    - from: "src/app/(auth)/login/google-button.tsx"
      to: "next-auth/react"
      via: "import { signIn } from 'next-auth/react'"
      pattern: "from 'next-auth/react'"
    - from: "src/app/(auth)/layout.tsx"
      to: "tailwind utilities"
      via: "Tailwind v3 processes globals.css → utilities for bg-gradient-to-br, backdrop-blur-xl, blur-3xl"
      pattern: "from-\\[#0D1B4B\\]"
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/app/(auth)/login/google-button.tsx"
      via: "import GoogleButton from './google-button'"
      pattern: "google-button"
---

<objective>
Deliver AUTH-02 (login screen with Pruma identity) and AUTH-03 (working Google OAuth click-through). Build the `/login` route as a Server Component (text + composition) plus one Client Component for the interactive Google button, wrapped in an `(auth)` route group layout that paints the navy gradient + glass atmosphere. Remove the legacy `page.jsx` re-exporting the old SPA component.

Purpose: UI-SPEC mandates exact pixel values, exact color hex literals, exact copy in pt-BR, and 5 explicit interaction states per interactive element. The page is the user's first impression — it must look intentional, not template-default. AUTH-03 requires the button to actually call `signIn('google')` so the OAuth flow completes end-to-end when env vars are present.

Output: Three new TSX files under `src/app/(auth)/` and the deletion of the legacy `.jsx` wrapper. Visual verification happens via human checkpoint at the end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-auth/04-UI-SPEC.md
@.planning/phases/04-auth/04-01-PLAN.md
@.planning/phases/04-auth/04-02-PLAN.md
@src/app/layout.tsx
@src/app/(auth)/login/page.jsx
@tailwind.config.ts

<interfaces>
<!-- Tailwind utilities expected to resolve (UI-SPEC §Color, §Interaction States, §Layout) -->

Arbitrary-value classes that must resolve via Tailwind v3 JIT (content glob from 04-01 covers `(auth)` because it is under `src/`):
- `bg-gradient-to-br from-[#0D1B4B] via-[#162460] to-[#1E3080]`
- `bg-[#00AEEF]/10`, `bg-[#5CCFF5]/8`
- `bg-white/10 backdrop-blur-xl border-white/20 rounded-2xl`
- `shadow-2xl shadow-black/30`
- `text-[#0D1B4B]`, `text-[#1F1F1F]`
- `text-[28px]`, `font-heading`, `font-sans`
- `focus-visible:ring-[#00AEEF] focus-visible:ring-offset-[#0D1B4B]`

NextAuth v5 client API (already installed as `next-auth@5.0.0-beta.25`):
```typescript
// src/app/(auth)/login/google-button.tsx (client)
import { signIn } from 'next-auth/react'
await signIn('google', { callbackUrl: '/dashboard' })
```

UI-SPEC layout structure (§Layout & Composition lines 211-228):
```tsx
<main className="relative min-h-screen overflow-hidden bg-gradient-to-br from-[#0D1B4B] via-[#162460] to-[#1E3080] flex items-center justify-center p-6 md:p-12">
  <div aria-hidden className="pointer-events-none absolute inset-0">
    <div className="absolute top-[-10%] right-[-10%] size-[480px] rounded-full bg-[#00AEEF]/10 blur-3xl" />
    <div className="absolute bottom-[-15%] left-[-10%] size-[520px] rounded-full bg-[#5CCFF5]/8 blur-3xl" />
  </div>
  <section className="relative z-10 w-full max-w-md ...">
    {/* page content */}
  </section>
  <footer className="absolute bottom-6 left-0 right-0 text-center text-sm text-white/60">
    © We Clínica · Sistema Financeiro interno
  </footer>
</main>
```

UI-SPEC copy contract (§Copywriting Contract lines 127-138, all pt-BR):
- Brand: "We Clínica" / "Sistema Financeiro"
- h1: "Bem-vindo de volta"
- Sub-headline: "Acesse o painel financeiro da clínica com sua conta Google autorizada."
- CTA default: "Continuar com Google"
- CTA loading: "Conectando…" (use real ellipsis character, not three dots)
- Helper: "Apenas usuários autorizados pela administração podem acessar."
- Footer: "© We Clínica · Sistema Financeiro interno"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the (auth) route group layout with gradient, blobs, and footer</name>
  <files>src/app/(auth)/layout.tsx</files>
  <read_first>
    - .planning/phases/04-auth/04-UI-SPEC.md (lines 207-228 — exact layout structure, lines 112-118 — gradient spec, lines 117-118 — blob spec, line 137 — footer copy)
    - tailwind.config.ts (confirm fontFamily.heading exists for the brand text in pages below)
    - src/app/layout.tsx (root layout — confirm `<body>` provides a clean foundation that this group layout overrides with `min-h-screen` + gradient)
  </read_first>
  <action>
    Create `src/app/(auth)/layout.tsx` as a Server Component (no `'use client'`).

    Default export `function AuthLayout({ children }: { children: React.ReactNode })` returning a single `<main>` element matching the UI-SPEC structure literally:

    1. `<main>` outer container: `className="relative min-h-screen overflow-hidden bg-gradient-to-br from-[#0D1B4B] via-[#162460] to-[#1E3080] flex items-center justify-center p-6 md:p-12"`

    2. Decorative blob layer `<div aria-hidden className="pointer-events-none absolute inset-0">` containing exactly two `<div>` blobs:
       - Top-right: `className="absolute top-[-10%] right-[-10%] size-[480px] rounded-full bg-[#00AEEF]/10 blur-3xl"`
       - Bottom-left: `className="absolute bottom-[-15%] left-[-10%] size-[520px] rounded-full bg-[#5CCFF5]/8 blur-3xl"`

    3. The `{children}` slot — wrap it in a `<div className="relative z-10 w-full max-w-md">` so pages render the card content directly without re-declaring the centering wrapper. Centering is handled by `flex items-center justify-center` on `<main>`, and `max-w-md` (UI-SPEC line 57 exception) caps the width.

    4. `<footer className="absolute bottom-6 left-0 right-0 text-center text-sm text-white/60">© We Clínica · Sistema Financeiro interno</footer>` — must be a real `©` glyph (Unicode U+00A9), NOT `&copy;` HTML entity (Next.js Server Components render entities literally).

    No imports needed beyond `React.ReactNode` type (implicit via JSX). Do NOT import `Metadata` here — the root layout owns title metadata.

    Do NOT add `'use client'`. Do NOT add Framer Motion or any animation library. Per UI-SPEC §Performance line 306, this whole layout is a Server Component.
  </action>
  <verify>
    <automated>test -f 'src/app/(auth)/layout.tsx' && grep -q 'from-\[#0D1B4B\]' 'src/app/(auth)/layout.tsx' && grep -q 'via-\[#162460\]' 'src/app/(auth)/layout.tsx' && grep -q 'to-\[#1E3080\]' 'src/app/(auth)/layout.tsx' && grep -q 'bg-\[#00AEEF\]/10' 'src/app/(auth)/layout.tsx' && grep -q 'bg-\[#5CCFF5\]/8' 'src/app/(auth)/layout.tsx' && grep -q 'blur-3xl' 'src/app/(auth)/layout.tsx' && grep -q 'min-h-screen' 'src/app/(auth)/layout.tsx' && grep -q 'max-w-md' 'src/app/(auth)/layout.tsx' && grep -q '© We Clínica · Sistema Financeiro interno' 'src/app/(auth)/layout.tsx' && ! grep -q "'use client'" 'src/app/(auth)/layout.tsx' && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(auth)/layout.tsx` exists and is a Server Component (NO `'use client'` directive on line 1 or anywhere in the file)
    - The outer `<main>` contains the exact gradient class string `from-[#0D1B4B] via-[#162460] to-[#1E3080]` AND `bg-gradient-to-br` AND `min-h-screen`
    - File contains exactly two blob `<div>`s — one with `bg-[#00AEEF]/10` and `size-[480px]`, one with `bg-[#5CCFF5]/8` and `size-[520px]`
    - Both blobs have `blur-3xl` and are inside a parent with `aria-hidden` and `pointer-events-none`
    - File contains the exact footer text `© We Clínica · Sistema Financeiro interno` with a real © glyph (not `&copy;`)
    - File wraps `{children}` in a container with `relative z-10 w-full max-w-md`
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    The `(auth)` route group has a Server Component layout that renders the Pruma navy gradient and atmosphere. Any page under this group inherits it automatically.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build the GoogleButton client component with all five interaction states</name>
  <files>src/app/(auth)/login/google-button.tsx</files>
  <read_first>
    - .planning/phases/04-auth/04-UI-SPEC.md (lines 171-179 — Button "Continuar com Google" full spec table, lines 281-283 — accessibility for the button + spinner)
    - .planning/phases/04-auth/04-02-PLAN.md (confirms `auth.ts` is the source — but client uses `next-auth/react`, not server `auth`)
  </read_first>
  <action>
    Create `src/app/(auth)/login/google-button.tsx`:

    1. First line: `'use client'`.

    2. Imports:
       - `useState` from 'react'
       - `signIn` from 'next-auth/react'
       - `Loader2` from 'lucide-react' (already in package.json — UI-SPEC line 30)

    3. Default export `function GoogleButton()`:
       - `const [loading, setLoading] = useState(false)`
       - `async function handleClick() { setLoading(true); await signIn('google', { callbackUrl: '/dashboard' }) }` — do NOT wrap in try/finally to reset loading; the OAuth flow either redirects away from the page (success) or NextAuth handles error display. If signIn somehow returns without redirecting (e.g. dev env vars missing), the spinner stays — acceptable for MVP.
       - Returns a `<button type="button" onClick={handleClick} disabled={loading} aria-label="Continuar com Google"></button>` with this exact className:
         `className="group relative flex h-12 w-full items-center justify-center gap-3 rounded-lg border border-white/40 bg-white px-6 text-base font-normal text-[#1F1F1F] shadow-lg shadow-black/20 transition-all duration-150 hover:-translate-y-px hover:bg-white/95 hover:shadow-xl active:translate-y-0 active:shadow-md focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#00AEEF] focus-visible:ring-offset-2 focus-visible:ring-offset-[#0D1B4B] disabled:cursor-not-allowed disabled:opacity-70"`
       - Inside the button, conditional render:
         - When `loading`: `<Loader2 aria-hidden className="size-4 animate-spin" />` followed by the text node `Conectando…` (use a real ellipsis character U+2026, NOT three periods)
         - When NOT loading: an inline Google "G" SVG (18×18, viewBox `0 0 48 48`, with the four official Google brand path colors — blue #4285F4, green #34A853, yellow #FBBC05, red #EA4335) marked `aria-hidden`, followed by the text node `Continuar com Google`

       The inline Google SVG MUST be hand-written (per UI-SPEC line 175: "ícone Google SVG colorido oficial (18px)" — UI-SPEC §Performance line 304 forbids adding image dependencies, and §Registry Safety §line 332 says no third-party brand-icon library is allowed). The four paths are public domain Google brand asset paths. Total file should remain under 80 lines.

    4. The text "Conectando…" and "Continuar com Google" are the only two visible string literals. Do not add tooltip props, `title`, or any other text.
  </action>
  <verify>
    <automated>test -f 'src/app/(auth)/login/google-button.tsx' && head -1 'src/app/(auth)/login/google-button.tsx' | grep -q "'use client'" && grep -q "from 'next-auth/react'" 'src/app/(auth)/login/google-button.tsx' && grep -q "signIn('google'" 'src/app/(auth)/login/google-button.tsx' && grep -q "callbackUrl: '/dashboard'" 'src/app/(auth)/login/google-button.tsx' && grep -q 'Loader2' 'src/app/(auth)/login/google-button.tsx' && grep -q 'animate-spin' 'src/app/(auth)/login/google-button.tsx' && grep -q 'Continuar com Google' 'src/app/(auth)/login/google-button.tsx' && grep -q 'Conectando…' 'src/app/(auth)/login/google-button.tsx' && grep -q 'h-12' 'src/app/(auth)/login/google-button.tsx' && grep -q 'focus-visible:ring-\[#00AEEF\]' 'src/app/(auth)/login/google-button.tsx' && grep -q 'disabled:opacity-70' 'src/app/(auth)/login/google-button.tsx' && grep -q 'aria-label="Continuar com Google"' 'src/app/(auth)/login/google-button.tsx' && grep -q '4285F4' 'src/app/(auth)/login/google-button.tsx' && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/(auth)/login/google-button.tsx` exists with `'use client'` as line 1
    - Imports `signIn` from `'next-auth/react'` (NOT from `@/lib/auth` — that is server-only)
    - Calls `signIn('google', { callbackUrl: '/dashboard' })` exactly
    - Button has the literal class strings `h-12`, `w-full`, `rounded-lg`, `bg-white`, `text-[#1F1F1F]`, `shadow-lg`, `shadow-black/20`
    - Button has hover state class `hover:-translate-y-px` AND `hover:bg-white/95`
    - Button has focus state class `focus-visible:ring-2 focus-visible:ring-[#00AEEF] focus-visible:ring-offset-2 focus-visible:ring-offset-[#0D1B4B]`
    - Button has active state class `active:translate-y-0 active:shadow-md`
    - Button has disabled state classes `disabled:opacity-70 disabled:cursor-not-allowed`
    - Button has `aria-label="Continuar com Google"` and `type="button"`
    - File renders `<Loader2 aria-hidden ... animate-spin />` when loading is true
    - File contains the literal string `Conectando…` (with real ellipsis U+2026, NOT three dots `...`)
    - File contains the literal string `Continuar com Google`
    - Inline Google SVG includes the four official brand hex colors `#4285F4`, `#34A853`, `#FBBC05`, `#EA4335` (case-insensitive match acceptable)
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    GoogleButton renders five interaction states per UI-SPEC, calls real signIn(), shows accessible loading state.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create /login page.tsx (Server Component) with card composition and remove the legacy page.jsx</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/login/page.jsx</files>
  <read_first>
    - .planning/phases/04-auth/04-UI-SPEC.md (lines 127-138 — copy contract for /login, lines 199-203 — card glassmorphism default state spec, lines 232-238 — vertical hierarchy inside the card)
    - src/app/(auth)/layout.tsx (created in Task 1 — confirm `max-w-md` wrapper is provided by layout so this page renders card content directly)
    - src/app/(auth)/login/page.jsx (current legacy file that re-exports the old SPA component — confirm contents before deleting)
  </read_first>
  <action>
    Create `src/app/(auth)/login/page.tsx` as a Server Component (no `'use client'`):

    1. Imports:
       - `import GoogleButton from './google-button'`

    2. Default export `function LoginPage()` returns a single `<section>` element with the glassmorphism card classes:
       `className="relative bg-white/10 backdrop-blur-xl border border-white/20 rounded-2xl shadow-2xl shadow-black/30 shadow-[0_0_60px_-12px_rgba(0,174,239,0.35)] p-8 md:p-10 animate-in fade-in zoom-in-95 duration-500 ease-out"` with `aria-labelledby="login-heading"`.

       Note: `animate-in fade-in zoom-in-95` are Tailwind utilities that exist in Tailwind v3 native — no plugin needed (they map to keyframes that ship with Tailwind v3.4). If lint warns, the build still succeeds; UI-SPEC says these animations should also collapse under `prefers-reduced-motion` (Tailwind v3 honors this automatically via the `motion-reduce:` variant — not required to add explicitly because base `animate-in` is wrapped in `@media (prefers-reduced-motion: no-preference)` by Tailwind plugin defaults; if not, accept the small regression and move on — UI-SPEC line 270 allows the entrance animation to be omitted under reduced motion).

    3. Vertical hierarchy inside the `<section>` (UI-SPEC lines 232-238). Use `flex flex-col` ordering with explicit gaps via margin classes (Tailwind's `gap-X` works on flex; UI-SPEC uses gap-2/4/6/8):

       a) Brand block: `<div className="flex flex-col items-center gap-2 mb-6">`
          - `<span className="text-[28px] font-bold font-heading leading-tight text-[#0D1B4B]">We Clínica</span>`
          - `<span className="text-sm font-normal font-sans text-[#0D1B4B]/70">Sistema Financeiro</span>`

       b) `<h1 id="login-heading" className="text-2xl font-bold font-heading leading-snug text-[#0D1B4B] text-center mb-2">Bem-vindo de volta</h1>`

       c) `<p className="text-base font-normal font-sans leading-normal text-slate-600 text-center mb-8">Acesse o painel financeiro da clínica com sua conta Google autorizada.</p>`

       d) `<GoogleButton />`

       e) `<p className="mt-4 text-sm font-normal font-sans text-slate-500 text-center">Apenas usuários autorizados pela administração podem acessar.</p>`

       Use real Unicode characters in pt-BR strings (`í`, `ó`, `ç`, etc.) — not HTML entities.

    4. DELETE `src/app/(auth)/login/page.jsx` (the legacy 2-line re-export). Run `git rm src/app/(auth)/login/page.jsx`. After deletion, verify Next.js resolves `/login` to the new `.tsx` (it does — extensions priority).

    Do not run `pnpm dev`. Visual verification happens in Task 4 of this plan (the next task).
  </action>
  <verify>
    <automated>test -f 'src/app/(auth)/login/page.tsx' && ! test -f 'src/app/(auth)/login/page.jsx' && grep -q 'from .\/google-button' 'src/app/(auth)/login/page.tsx' && grep -q 'bg-white/10' 'src/app/(auth)/login/page.tsx' && grep -q 'backdrop-blur-xl' 'src/app/(auth)/login/page.tsx' && grep -q 'border-white/20' 'src/app/(auth)/login/page.tsx' && grep -q 'rounded-2xl' 'src/app/(auth)/login/page.tsx' && grep -q 'shadow-2xl' 'src/app/(auth)/login/page.tsx' && grep -q 'aria-labelledby="login-heading"' 'src/app/(auth)/login/page.tsx' && grep -q 'id="login-heading"' 'src/app/(auth)/login/page.tsx' && grep -q 'We Clínica' 'src/app/(auth)/login/page.tsx' && grep -q 'Sistema Financeiro' 'src/app/(auth)/login/page.tsx' && grep -q 'Bem-vindo de volta' 'src/app/(auth)/login/page.tsx' && grep -q 'Acesse o painel financeiro da clínica' 'src/app/(auth)/login/page.tsx' && grep -q 'Apenas usuários autorizados' 'src/app/(auth)/login/page.tsx' && grep -q 'font-heading' 'src/app/(auth)/login/page.tsx' && grep -q 'text-\[#0D1B4B\]' 'src/app/(auth)/login/page.tsx' && ! grep -q "'use client'" 'src/app/(auth)/login/page.tsx' && pnpm exec tsc --noEmit && pnpm run build 2>&1 | tee /tmp/04-03-build.log | grep -qE "(Compiled successfully|✓ Compiled)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(auth)/login/page.tsx` exists and is a Server Component (no `'use client'`)
    - Legacy file `src/app/(auth)/login/page.jsx` no longer exists
    - Page imports `GoogleButton` from `./google-button`
    - Card `<section>` has classes `bg-white/10`, `backdrop-blur-xl`, `border-white/20`, `rounded-2xl`, `shadow-2xl`, `p-8 md:p-10`
    - Card has `aria-labelledby="login-heading"` attribute
    - `<h1 id="login-heading">` exists with text `Bem-vindo de volta`
    - Page contains all five required copy strings literally (UTF-8, no entities): `We Clínica`, `Sistema Financeiro`, `Bem-vindo de volta`, `Acesse o painel financeiro da clínica com sua conta Google autorizada.`, `Apenas usuários autorizados pela administração podem acessar.`
    - Brand "We Clínica" uses `text-[28px]`, `font-bold`, `font-heading` classes
    - h1 uses `text-2xl`, `font-bold`, `font-heading` classes
    - Body paragraph uses `text-base`, `font-normal`, `font-sans`
    - `pnpm exec tsc --noEmit` exits 0
    - `pnpm run build` succeeds and emits `Compiled successfully` (or `✓ Compiled`)
    - Build CSS output under `.next/static/css/` contains the literal class `from-[#0D1B4B]` (proves Tailwind processed the auth layout): verify with `find .next/static/css -name '*.css' -exec grep -l 'from-\[#0D1B4B\]' {} +` — at least one match
  </acceptance_criteria>
  <done>
    `/login` route resolves to a clean Server Component composition with the GoogleButton client embedded. Legacy SPA wrapper deleted. Project builds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of /login visual + OAuth flow</name>
  <what-built>
    A new `/login` route under the `(auth)` group with: navy gradient background, two blur blobs, centered glassmorphism card containing the brand "We Clínica · Sistema Financeiro", h1 "Bem-vindo de volta", sub-headline, "Continuar com Google" button (white with the four-color Google G icon), and helper text. Clicking the button calls `signIn('google', { callbackUrl: '/dashboard' })`.
  </what-built>
  <how-to-verify>
    Run automated checks first (Claude does these for the user):
    1. `pnpm run typecheck` exits 0
    2. `pnpm run build` exits 0

    Then the user must verify manually because Phase 4 requires AUTH-03 (working OAuth round-trip) and that depends on env vars + Google Cloud Console configuration that only the user can provide.

    Manual steps for the user:
    1. Confirm AUTH_* env vars are configured in `.env.local` for local dev (AUTH_SECRET, AUTH_URL=http://localhost:3000, AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET). If not, set them now using the user_setup instructions from Plan 04-02.
    2. Confirm Google Cloud Console has http://localhost:3000 in Authorized JavaScript origins and http://localhost:3000/api/auth/callback/google in Authorized redirect URIs.
    3. Run `pnpm run dev` and open http://localhost:3000/login in a browser.
    4. Visual check (UI-SPEC contract):
       - Background is a navy gradient running from top-left dark navy to bottom-right slightly lighter navy
       - Two soft cyan/blue blurred blobs visible in opposite corners (top-right and bottom-left)
       - A glassmorphism card is centered, semi-transparent white, with a soft cyan glow
       - "We Clínica" appears in Barlow bold ~28px, with "Sistema Financeiro" beneath it
       - h1 "Bem-vindo de volta" is below the brand
       - The Google button is white, full-width inside the card, h-12, with the colored Google G icon on the left
       - Helper text "Apenas usuários autorizados…" is below the button
       - Footer "© We Clínica · Sistema Financeiro interno" is at the bottom of the viewport
    5. Interaction check:
       - Tab to the Google button — focus ring is cyan (#00AEEF)
       - Hover the button — it lifts slightly and gains a stronger shadow
    6. OAuth flow check:
       - Click "Continuar com Google" — button shows "Conectando…" with a spinning Loader2 icon
       - Browser redirects to Google's account chooser
       - Pick the whitelisted account (marcelo.mattioli@pruma.io seeded in Phase 3)
       - You should land on `/dashboard` with an active session (or a 404 if dashboard route does not exist yet — that's fine for Phase 4; what matters is that `/login` does NOT reload itself, which would indicate the session was minted successfully)
       - Refresh the browser — you should NOT be sent back to /login (proves AUTH-05 session persistence)
    7. Negative-path check (AUTH-04, verified more fully by Plan 04-04):
       - Sign out (clear cookies or visit `/api/auth/signout`)
       - Click "Continuar com Google" with a Google account NOT in the whitelist
       - You should be redirected to `/acesso-negado?email=...` (this page is rendered by Plan 04-04 — for now, just confirming the URL appears even if the page returns 404)

    If any visual check fails (gradient colors wrong, card not glassmorphism, fonts wrong), report the discrepancy and Claude will fix before continuing.

    If OAuth fails because env vars aren't set, fix that first (see user_setup in 04-02-PLAN.md) — this is not a code defect.
  </how-to-verify>
  <resume-signal>Type "approved" when /login looks correct AND the OAuth flow works for a whitelisted account, or describe specific issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /login | Anonymous public page; nothing sensitive rendered |
| GoogleButton → next-auth/react signIn | Triggers OAuth redirect to Google; PKCE + state handled by NextAuth |
| OAuth callback → /api/auth/callback/google | Back-channel + front-channel validation by NextAuth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Spoofing | callbackUrl parameter to signIn() | mitigate | callbackUrl is a HARDCODED string literal `'/dashboard'`, not user-controlled. NextAuth additionally validates that callbackUrl resolves to the same origin |
| T-04-03-02 | Information disclosure | Inline Google brand SVG | accept | The Google "G" logo paths are public brand assets; no trademark/policy violation for displaying them in OAuth-consenting auth UI (Google's brand guidelines explicitly permit) |
| T-04-03-03 | Tampering | Loading state can't be reset client-side | accept | If signIn() returns without redirecting (env var misconfig in dev), button stays disabled — user can refresh. Acceptable degradation; surface in checkpoint |
| T-04-03-04 | Information disclosure | XSS via copy strings | mitigate | All copy is static literal in TSX (React auto-escapes). No `dangerouslySetInnerHTML` used. No user input rendered on this page |
| T-04-03-SC | Tampering | No new package installs in this plan | accept | Uses `next-auth@5.0.0-beta.25` and `lucide-react` already installed in Phase 3 / earlier; no legitimacy gate triggered |
</threat_model>

<verification>
- Static structural verification: grep + tsc + build in Tasks 1-3
- Visual + interactive verification: Task 4 human checkpoint
- AUTH-02 (Pruma identity): satisfied by the layout + page composition matching UI-SPEC structure
- AUTH-03 (OAuth flow complete): satisfied when human verification confirms the end-to-end click → consent → callback → session round-trip
</verification>

<success_criteria>
- All three code tasks' acceptance_criteria satisfied
- Production build (`pnpm run build`) exits 0
- Human checkpoint (Task 4) returns "approved"
- `/login` visually matches UI-SPEC layout, copy, color, typography
- A whitelisted Google account completes OAuth and lands with a valid session
- A non-whitelisted Google account is redirected to `/acesso-negado?email=...` (page itself comes from 04-04, but the redirect target proves the signIn callback works)
</success_criteria>

<output>
Create `.planning/phases/04-auth/04-03-SUMMARY.md` documenting: files created, the deletion of `page.jsx`, screenshots or text description of the rendered /login if Task 4 captured them, and explicit confirmation of which Google account was used to verify the OAuth round-trip (whitelisted vs. not).
</output>
