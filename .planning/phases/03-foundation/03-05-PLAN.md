---
phase: 03-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - 03-01
  - 03-04
files_modified:
  - vercel.ts
autonomous: false
requirements:
  - INFRA-04
  - INFRA-05
  - INFRA-06
must_haves:
  truths:
    - "vercel.ts buildCommand uses pnpm (D-04)"
    - "Production deploy is reachable at the public Vercel URL with HTTP 200 / 302 / 307"
    - "Production deploy build logs show `pnpm install` and `pnpm run build` (not npm)"
    - "Push to main triggers a production deploy automatically"
    - "Opening a PR (or pushing to a non-main branch) triggers a preview deploy automatically"
    - "All required Vercel env vars are present in Production AND Preview environments"
  artifacts:
    - path: "vercel.ts"
      provides: "Vercel project config with pnpm build command and Next.js framework preset"
      contains: "pnpm run build"
  key_links:
    - from: "vercel.ts"
      to: "Vercel build"
      via: "buildCommand override"
      pattern: "pnpm run build"
    - from: "main branch push"
      to: "production deploy"
      via: "Vercel git integration"
      pattern: "Production"
---

<objective>
Update vercel.ts to use pnpm, push the v2.0 foundation to main, verify the production deploy succeeds, and validate the PR → preview deploy flow.

Purpose: This plan delivers INFRA-04 (Vercel deploy reachable), INFRA-06 (push to main auto-deploys, PRs preview-deploy), and confirms INFRA-05 (env vars complete). Without this validation, the phase cannot claim success — the roadmap's Success Criterion 3 requires "App está acessível via URL pública do Vercel com build passando".

Output: Production deploy live at the project's Vercel URL with pnpm-driven build logs, a successful preview deploy from a test PR, and confirmation that all required env vars are populated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-foundation/03-CONTEXT.md
@.planning/phases/03-foundation/03-PATTERNS.md
@.planning/phases/03-foundation/03-01-SUMMARY.md
@.planning/phases/03-foundation/03-03-SUMMARY.md
@.planning/phases/03-foundation/03-04-SUMMARY.md

@vercel.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update vercel.ts buildCommand from npm to pnpm</name>
  <files>vercel.ts</files>
  <read_first>
    - vercel.ts (current content — framework already 'nextjs', buildCommand currently 'npm run build')
    - .planning/phases/03-foundation/03-PATTERNS.md (section "`vercel.ts`" lines 240-263)
    - .planning/phases/03-foundation/03-CONTEXT.md (D-04 npm to pnpm)
  </read_first>
  <action>
    Change the `buildCommand` value in `vercel.ts` from `'npm run build'` to `'pnpm run build'`. Keep:
    - import line unchanged
    - `framework: 'nextjs'` unchanged
    - `outputDirectory: '.next'` unchanged

    Do NOT introduce any other fields. Vercel auto-detects pnpm from `packageManager` in package.json (set in Plan 01) AND `pnpm-lock.yaml` presence, but explicit buildCommand prevents the platform from falling back to npm if detection misses.
  </action>
  <verify>
    <automated>grep -q "buildCommand: 'pnpm run build'" vercel.ts && grep -q "framework: 'nextjs'" vercel.ts && grep -q "outputDirectory: '.next'" vercel.ts && ! grep -q "npm run build" vercel.ts && pnpm exec tsc --noEmit vercel.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vercel.ts` contains the literal string `buildCommand: 'pnpm run build'`
    - `vercel.ts` still contains `framework: 'nextjs'`
    - `vercel.ts` still contains `outputDirectory: '.next'`
    - `vercel.ts` does NOT contain `npm run build` anywhere
    - `pnpm exec tsc --noEmit vercel.ts` exits 0
  </acceptance_criteria>
  <done>vercel.ts uses pnpm for the build command. File still type-checks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit Phase 3 foundation and push to main, monitor production deploy</name>
  <files>(no source files — git + Vercel)</files>
  <read_first>
    - .git/config (verify remote 'origin' points to the expected repo)
    - .planning/phases/03-foundation/03-01-SUMMARY.md (confirm Plan 01 work is committed or staged)
    - .planning/phases/03-foundation/03-02-SUMMARY.md (Plan 02 work)
    - .planning/phases/03-foundation/03-04-SUMMARY.md (Plan 04 work — DB applied)
  </read_first>
  <action>
    Pre-flight check: run `git status` and confirm the working tree contains the cumulative changes from Plans 01, 02, and 04 (plus this plan's vercel.ts change). Files expected:
    - package.json, pnpm-lock.yaml (Plan 01)
    - tsconfig.json, next-env.d.ts (Plan 01)
    - src/app/layout.tsx, src/app/page.tsx, middleware.ts (Plan 01)
    - DELETED: src/app/layout.jsx, src/app/page.jsx, middleware.js, package-lock.json (Plan 01)
    - src/db/schema.ts, src/lib/db.ts, drizzle.config.ts (Plan 02)
    - .env.example (Plan 02 — note .env.local is gitignored, do not stage)
    - drizzle/0001_seed.sql (Plan 04)
    - vercel.ts (this plan, Task 1)
    - .planning/phases/03-foundation/03-0X-SUMMARY.md files (from each plan)

    Pre-flight verify: `git status` MUST NOT show any `.env.local` or other secret-bearing file as modified-and-staged. Run `git diff --cached --name-only | grep -E "^\.env\.(local|production)" ; [ $? -ne 0 ]` to assert no env files are staged. If any env file appears, abort and unstage with `git restore --staged <file>`.

    Stage with explicit paths (not `git add -A`):
    ```
    git add package.json pnpm-lock.yaml tsconfig.json next-env.d.ts \
            src/app/layout.tsx src/app/page.tsx middleware.ts \
            src/db/schema.ts src/lib/db.ts drizzle.config.ts \
            .env.example drizzle/0001_seed.sql vercel.ts \
            .planning/phases/03-foundation/
    git add -u    # captures deletions of layout.jsx, page.jsx, middleware.js, package-lock.json
    git status    # final review
    ```

    Run `pnpm run build` locally one last time to fail fast before pushing. If it exits 0, proceed.

    Commit with conventional message:
    ```
    git commit -m "feat(03): foundation — Next.js TS + pnpm + Drizzle + Neon

    - Migrate to pnpm and TypeScript strict mode (D-04, D-05, D-06)
    - Add @/* path alias to tsconfig (D-07)
    - Port root layout, root page, and middleware to TS
    - Drizzle schema for 8 tables with D-08 column renames
    - Neon serverless client singleton
    - drizzle-kit push applied; seed + first admin (marcelo.mattioli@pruma.io) loaded
    - Update vercel.ts buildCommand to pnpm

    Closes INFRA-01, INFRA-02, INFRA-03, INFRA-05 (partial), DB-01, DB-02, DB-03"
    ```

    Push to main:
    ```
    git push origin main
    ```

    Vercel will auto-deploy. Monitor the deploy using the Vercel CLI:
    ```
    # The vercel CLI is in dependencies — invoke via pnpm
    pnpm exec vercel inspect --logs <deployment-url-or-id>
    ```

    OR query the deploy list:
    ```
    pnpm exec vercel ls --prod
    ```

    Wait for the production deploy to reach state READY (use `until` with `vercel ls --prod | head -2 | grep -q READY`, max 5 minutes — Vercel typically completes a Next.js build in 60-180 seconds for this size project).

    Once READY, capture:
    - The production URL (e.g., `https://we-clinica-financeiro.vercel.app` or custom domain)
    - A snippet of build logs showing `pnpm install` and `pnpm run build` (NOT `npm install`)

    Validate the deployed URL:
    ```
    curl -s -o /dev/null -w "%{http_code}" https://<production-url>
    ```
    Acceptable codes: 200 (page rendered), 302 / 307 (redirect to /login since no session). 500 or build failure → halt.
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -q "feat(03)" && git status --porcelain | grep -E "^.. (.env\.local|.env\.production)" | wc -l | awk '$1 == 0 {exit 0} {exit 1}' && pnpm exec vercel ls --prod 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format=%s` returns a message starting with `feat(03)`
    - `git status` shows a clean working tree post-commit (no leftover modified files)
    - `git diff HEAD~1 --name-only` includes: package.json, pnpm-lock.yaml, tsconfig.json, src/db/schema.ts, src/lib/db.ts, drizzle.config.ts, vercel.ts, drizzle/0001_seed.sql
    - `git diff HEAD~1 --name-only` does NOT include `.env.local` or any other secret file
    - `pnpm exec vercel ls --prod` shows a deployment with state `Ready` within 5 minutes of push
    - `curl -s -o /dev/null -w "%{http_code}" https://<production-url>` returns 200, 302, or 307
    - Vercel build logs (captured via `vercel inspect --logs <id>`) contain the literal strings `pnpm` AND `pnpm install` AND `pnpm run build`
    - Vercel build logs do NOT contain `npm install` (case-sensitive grep)
  </acceptance_criteria>
  <done>Production deploy is live, pnpm-driven build confirmed in logs, public URL responds with 200/302/307. INFRA-04 verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify PR preview deploy + final env var sanity check</name>
  <files>(no files — Vercel + GitHub)</files>
  <read_first>
    - .planning/phases/03-foundation/03-03-SUMMARY.md (whether AUTH_* env vars were added to Vercel in Plan 03)
  </read_first>
  <what-built>
    Production deploy is live and pnpm-driven. INFRA-06 requires that PRs (or pushes to non-main branches) also trigger preview deploys. INFRA-05 requires the four NextAuth env vars present in Vercel for Production AND Preview. This task asks the user to validate both via the Vercel dashboard / a quick test PR.
  </what-built>
  <how-to-verify>
    Step A — Preview deploy validation (INFRA-06):

    The executor will create a tiny throwaway branch, push it, and the user verifies the preview deploy appears. The executor runs:
    ```
    git checkout -b chore/verify-preview-03
    # touch a no-op file to force a push that triggers a preview
    echo "// preview verification — safe to delete" > /tmp/noop.txt
    cp /tmp/noop.txt .planning/phases/03-foundation/.preview-test
    git add .planning/phases/03-foundation/.preview-test
    git commit -m "chore: trigger preview deploy verification"
    git push -u origin chore/verify-preview-03
    ```

    Then user must:
    1. Open https://vercel.com/dashboard → project `we-clinica-financeiro` → Deployments tab.
    2. Confirm a NEW deployment appears with Environment = "Preview" and Source = `chore/verify-preview-03`.
    3. Wait for it to reach `Ready` (typically 60-180s).
    4. Click the deployment URL (looks like `we-clinica-financeiro-<hash>-<scope>.vercel.app`) → confirm the page loads (200/302/307 is fine).
    5. Report: "preview deploy verified at <url>"

    Step B — Env var sanity (INFRA-05):

    User opens Vercel → Settings → Environment Variables and confirms ALL of these have checkmarks for BOTH **Production** AND **Preview**:
    - `DATABASE_URL` (added by Neon Marketplace in Plan 03)
    - `AUTH_SECRET`
    - `AUTH_URL` (Production: `https://<production-url>`, Preview: `$VERCEL_URL` or `https://$VERCEL_URL` is acceptable — Vercel injects VERCEL_URL automatically per deploy)
    - `AUTH_GOOGLE_ID`
    - `AUTH_GOOGLE_SECRET`

    If any AUTH_* var is missing, user adds it now using the values from the local `.env.local`. The AUTH_GOOGLE_ID / AUTH_GOOGLE_SECRET may be the same as v1.0 if the OAuth consent screen domain still matches. If the Vercel production URL changed, the user must update the Google Cloud Console OAuth callback URI (this can be deferred to Phase 4 since no /login route exists yet).

    After verification, executor cleans up:
    ```
    git checkout main
    git push origin --delete chore/verify-preview-03    # delete remote branch
    git branch -D chore/verify-preview-03               # delete local branch
    rm .planning/phases/03-foundation/.preview-test     # remove on main if accidentally merged
    ```
    The .preview-test file lives only on the throwaway branch and is removed when the branch is deleted.
  </how-to-verify>
  <resume-signal>
    User types one of:
    - "preview verified — env vars complete" (Step A + Step B both passed)
    - "preview verified — env vars incomplete: <list>" (Step A passed, Step B has gaps — executor records as known follow-up for Phase 4)
    - "preview failed — <reason>" (executor investigates)
  </resume-signal>
  <acceptance_criteria>
    - User confirms a preview deploy from branch `chore/verify-preview-03` reached state Ready and the URL responds (any 2xx/3xx code)
    - User confirms DATABASE_URL, AUTH_SECRET, AUTH_URL, AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET are present in Vercel for BOTH Production AND Preview environments
    - The throwaway branch `chore/verify-preview-03` is deleted from origin after verification (`git ls-remote origin chore/verify-preview-03` returns empty)
    - If any AUTH_* env vars remain missing, executor records the exact missing list in SUMMARY.md and surfaces it as a Phase 4 prerequisite
  </acceptance_criteria>
  <done>INFRA-06 verified (preview deploy works). INFRA-05 verified or partial-with-known-gaps. Throwaway branch cleaned up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git → github remote | push uses developer's git credentials |
| github → vercel | git integration auto-triggers builds |
| vercel build → DATABASE_URL | build does not need DB at build time, but runtime does |
| google oauth callback → vercel domain | callback URI must match registered domain in Google Cloud Console |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Information Disclosure | .env.local accidentally committed | mitigate | Task 2 pre-flight asserts no `.env.local` is staged before commit. Uses explicit paths to `git add`, not `git add -A`. |
| T-03-20 | Tampering | npm fallback used in Vercel build | mitigate | vercel.ts explicit `buildCommand: 'pnpm run build'` + acceptance criterion greps build logs for `pnpm install` and absence of `npm install`. |
| T-03-21 | Spoofing | Google OAuth callback for production fails (wrong URI) | accept | Phase 4 owns the auth flow. AUTH_URL env var documented in Task 3 but actual /login route doesn't exist yet — no functional auth check possible in Phase 3. |
| T-03-22 | Tampering | preview deploy uses wrong env vars and hits production Neon | accept | INFRA-05 requires Preview env vars to be the SAME as Production (D-12 marketplace integration applies DATABASE_URL to both). For v2.0 there's no separate staging Neon — preview deploys legitimately read/write production data. Documented limitation; mitigated by zero data in DB at Phase 3 end (only seed rows). |
| T-03-23 | Denial of Service | Vercel build hangs indefinitely | mitigate | Task 2 has a 5-minute timeout. Executor halts and surfaces if build does not complete. |
</threat_model>

<verification>
- vercel.ts uses pnpm
- Production deploy responds with 2xx/3xx
- Build logs show pnpm (not npm)
- Push to main → Production deploy (auto)
- Push to non-main branch → Preview deploy (auto)
- DATABASE_URL + AUTH_* env vars in Vercel for Production + Preview
- Throwaway preview branch is cleaned up
</verification>

<success_criteria>
- INFRA-04 satisfied: Vercel deploy reachable with build passing
- INFRA-05 satisfied: All required env vars in Vercel (DATABASE_URL + 4 AUTH_*)
- INFRA-06 satisfied: main push → production deploy, PR/branch push → preview deploy
- All Phase 3 ROADMAP Success Criteria met (1-5)
</success_criteria>

<output>
Create `.planning/phases/03-foundation/03-05-SUMMARY.md` when done. Include:
- Production deploy URL and HTTP status code from curl
- Build log snippet showing `pnpm install` and `pnpm run build`
- Preview deploy URL and status
- List of env vars confirmed in Vercel for Production AND Preview
- Any AUTH_* vars not yet added (deferred to Phase 4)
- Commit SHA on main
- Phase 3 completion confirmation against ROADMAP Success Criteria 1-5
</output>
