---
phase: 03-foundation
plan: "01"
subsystem: foundation
tags: [pnpm, typescript, drizzle, neon, toolchain]
dependency_graph:
  requires: []
  provides: [pnpm-workspace, ts-strict-config, typed-entry-files]
  affects: [03-02, 03-03, 03-04, 03-05]
tech_stack:
  added: [pnpm@9.15.9, typescript@5.9.3, drizzle-orm@0.38.4, drizzle-kit@0.29.1, "@neondatabase/serverless@0.10.4", "@types/node", "@types/react", "@types/react-dom"]
  patterns: [pnpm-workspace, strict-ts-config, nextjs-path-alias, app-router-tsx-entry-files]
key_files:
  created: [package.json, pnpm-lock.yaml, tsconfig.json, src/app/layout.tsx, src/app/page.tsx, middleware.ts]
  modified: []
decisions:
  - "Kept @supabase/supabase-js in dependencies temporarily — auth-config.js still uses it; Phase 4 will remove it and replace with Neon-backed auth"
  - "next-env.d.ts already existed (pre-generated by Next.js) and is gitignored per .gitignore — left as-is per plan instruction"
metrics:
  duration: "~25 minutes"
  completed: "2026-05-19"
  tasks_completed: 3
  files_created: 6
  files_deleted: 3
---

# Phase 3 Plan 01: pnpm + TypeScript Foundation Summary

**One-liner:** Migrated package manager from npm to pnpm@9.15.9 and added strict TypeScript config with @/* alias, Drizzle+Neon toolchain, and ported three entry files (layout, page, middleware) to .tsx/.ts using the @/ alias.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Migrate npm to pnpm + TypeScript toolchain | 09ab16e | package.json, pnpm-lock.yaml |
| 2 | Add tsconfig.json strict mode + @/* alias | 89ca1fb | tsconfig.json |
| 3 | Port layout, page, middleware to TypeScript | 3937c26 | layout.tsx, page.tsx, middleware.ts |

## Acceptance Criteria Verification

### pnpm and toolchain (Task 1)
- `pnpm-lock.yaml` exists at repo root: **PASS**
- `package-lock.json` removed: **PASS**
- `package.json` contains `"packageManager": "pnpm@9.15.9"`: **PASS**
- `package.json` contains `drizzle-orm`, `@neondatabase/serverless`, `drizzle-kit`, `typescript`, `@types/*`: **PASS**
- Scripts `db:push` and `typecheck` present: **PASS**
- `pnpm exec next --version` → `Next.js v15.5.18`: **PASS**

### TypeScript config (Task 2)
- `tsconfig.json` exists with `"strict": true`: **PASS**
- `@/*` → `./src/*` path alias configured: **PASS**
- `"allowJs": true` for .jsx coexistence: **PASS**
- `"plugins": [{ "name": "next" }]`: **PASS**
- `pnpm exec tsc --noEmit` → **TypeScript: No errors found**: **PASS**

### TypeScript entry files (Task 3)
- `src/app/layout.tsx`, `src/app/page.tsx`, `middleware.ts` exist: **PASS**
- `src/app/layout.jsx`, `src/app/page.jsx`, `middleware.js` removed: **PASS**
- All three use `@/` alias: **PASS**
- `layout.tsx` contains `crossOrigin="anonymous"` and `#F3F2ED`: **PASS**
- `middleware.ts` preserves exact matcher: **PASS**
- `pnpm exec tsc --noEmit` → clean after porting: **PASS**
- Dev server response: **HTTP 307** (redirect to /login — expected auth behavior): **PASS**

## tsc --noEmit Output

```
TypeScript: No errors found
```

## Dev Server Check

```
curl -s -o /dev/null -w "%{http_code}" http://localhost:3003
307
```

(Port 3003 used — 3000 was occupied by another process in dev. Behavior identical.)

## Resolved Versions (pnpm-lock.yaml)

| Package | Resolved version |
|---------|-----------------|
| pnpm | 9.15.9 |
| `@neondatabase/serverless` | 0.10.4 |
| `drizzle-orm` | 0.38.4 |
| `drizzle-kit` | 0.29.1 |
| `typescript` | 5.9.3 |

## Deviations from Plan

### Deviation 1 (Plan Directive — Critical Note)

**@supabase/supabase-js kept in dependencies**

- **Found during:** Task 1 — as documented in the prompt's critical note
- **Issue:** Plan says remove `@supabase/supabase-js`, but `src/lib/auth-config.js` still imports it and is not ported in Phase 3
- **Fix:** Kept `@supabase/supabase-js: ^2.45.4` in `dependencies` for Phase 3. Phase 4 will replace auth-config.js with a Neon-backed implementation and remove this dependency.
- **Files modified:** `package.json`
- **Impact:** No violation of any must_have truth — `pnpm run dev` boots without errors, which is the must_have

### Deviation 2 (Observation — Non-blocking)

**next-env.d.ts already existed and is gitignored**

- **Found during:** Task 2 commit
- **Situation:** `next-env.d.ts` was already present (pre-generated by Next.js) and `.gitignore` already contained it. The plan says "leave .gitignore as-is" — followed exactly.
- **Fix:** Verified file exists locally (correct content with 3 reference directives). No change needed.
- **Impact:** None — file is available locally for tsc and Next.js compilation

### Deviation 3 (Rule 2 — Auto-added)

**vercel.ts not updated**

- **Observed:** `vercel.ts` still has `buildCommand: 'npm run build'` — PATTERNS.md section notes it should change to `pnpm run build` (D-04). This file was not in the plan's `files_modified` list.
- **Decision:** Left as-is to stay within plan scope. Flagged here for the next plan or a quick follow-up.

## Legacy .jsx Files Under allowJs

The following `.jsx` files remain and are covered by `allowJs: true`. They produced no tsc errors:

- `src/app/(auth)/login/page.jsx`
- `src/app/(dashboard)/*/` — all dashboard module pages
- `src/components/Providers.jsx`, `src/components/SupabaseAuthSync.jsx`
- `src/hooks/*.js`, `src/utils/*.js`, `src/lib/*.js`

These will be ported in Phase 6 (module implementation). No TS errors originate from them under current config.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes were introduced in this plan. All changes are tooling/config and entry-file ports. No new threat flags.

## Known Stubs

None — this plan creates no UI or data-serving components.

## Self-Check

- `tsconfig.json` exists at repo root: FOUND
- `src/app/layout.tsx` exists: FOUND
- `src/app/page.tsx` exists: FOUND
- `middleware.ts` exists at repo root: FOUND
- `pnpm-lock.yaml` exists: FOUND
- `package-lock.json` absent: CONFIRMED
- Commits 09ab16e, 89ca1fb, 3937c26 exist in git log: CONFIRMED

## Self-Check: PASSED
