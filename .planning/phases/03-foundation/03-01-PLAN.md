---
phase: 03-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next-env.d.ts
  - src/app/layout.tsx
  - src/app/layout.jsx
  - src/app/page.tsx
  - src/app/page.jsx
  - middleware.ts
  - middleware.js
  - next.config.js
autonomous: true
requirements:
  - INFRA-01
must_haves:
  truths:
    - "Project uses pnpm as package manager (pnpm-lock.yaml exists, package-lock.json removed)"
    - "TypeScript strict mode active and `pnpm exec tsc --noEmit` exits 0"
    - "`pnpm run dev` boots Next.js on localhost:3000 without errors"
    - "Path alias `@/*` resolves to `src/*` in tsconfig"
    - "Root layout, root page, and middleware are TypeScript (.ts/.tsx)"
  artifacts:
    - path: "tsconfig.json"
      provides: "TypeScript strict config with @/* path alias"
      contains: "\"strict\": true"
    - path: "package.json"
      provides: "pnpm scripts, TS devDeps, drizzle scripts placeholder"
      contains: "drizzle-kit"
    - path: "pnpm-lock.yaml"
      provides: "pnpm dependency lock"
    - path: "src/app/layout.tsx"
      provides: "Typed root layout component"
    - path: "src/app/page.tsx"
      provides: "Typed root async server component with auth redirect"
    - path: "middleware.ts"
      provides: "Typed Next.js auth middleware"
  key_links:
    - from: "tsconfig.json"
      to: "src/*"
      via: "paths alias"
      pattern: "\"@/\\*\": \\[\"./src/\\*\"\\]"
    - from: "package.json"
      to: "pnpm"
      via: "packageManager + scripts"
      pattern: "pnpm"
---

<objective>
Migrate project foundation from npm + JavaScript to pnpm + TypeScript strict mode while preserving the existing Next.js App Router structure.

Purpose: Establish the type-safe, pnpm-managed baseline that all subsequent v2.0 work depends on (Drizzle schema in Plan 02, Auth in Phase 4, modules in Phase 6). Without this, no other Phase 3 task can deliver type inference from Drizzle or run `pnpm` commands.

Output: Repo boots with `pnpm dev` on `localhost:3000`, `tsc --noEmit` is clean, and the root layout/page/middleware are TypeScript.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-foundation/03-CONTEXT.md
@.planning/phases/03-foundation/03-PATTERNS.md

@package.json
@vercel.ts
@src/app/layout.jsx
@src/app/page.jsx
@middleware.js
@next.config.js

<interfaces>
Existing imports the typed files MUST preserve:

From src/lib/auth-config.js (kept as-is in Phase 3 — Phase 4 will port to TS):
  - export `auth` (NextAuth v5 handler used by middleware.js, src/app/page.jsx)

From src/components/Providers.jsx (kept as-is in Phase 3):
  - default export `Providers` React component, accepts `children`

Root layout body styling (preserve exactly during port):
  - margin: 0, padding: 0
  - fontFamily: "'DM Sans', system-ui, -apple-system, sans-serif"
  - background: '#F3F2ED'
  - color: '#1A1918'
  - WebkitFontSmoothing: 'antialiased'

Google Fonts link tag in <head> (preserve exactly):
  - DM Sans weights 400,500,600,700 via fonts.googleapis.com
  - preconnect to fonts.googleapis.com and fonts.gstatic.com (crossOrigin="anonymous" in TSX)

Middleware matcher (preserve exactly):
  - matcher: ['/((?!api/auth|_next/static|_next/image|favicon.ico).*)']
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate package manager npm to pnpm with TypeScript toolchain</name>
  <files>package.json, package-lock.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (current scripts and deps)
    - package-lock.json (verify lockfileVersion before removing)
    - .planning/phases/03-foundation/03-PATTERNS.md (section "`package.json` (config, —)" lines 308-368 for exact dep versions)
    - .planning/phases/03-foundation/03-CONTEXT.md (D-04 pnpm decision, D-05 TS decision)
  </read_first>
  <action>
    Per D-04 (npm to pnpm) and D-05 (full TypeScript), rewrite package.json with:
    - Add `"packageManager": "pnpm@9.x"` field at top level (use a current 9.x version)
    - scripts: keep `dev`, `build`, `start`; add `db:generate` ("drizzle-kit generate"), `db:push` ("drizzle-kit push"), `db:studio` ("drizzle-kit studio"), `typecheck` ("tsc --noEmit")
    - dependencies: KEEP `next ^15.3.2`, `next-auth ^5.0.0-beta.25`, `react ^18.3.1`, `react-dom ^18.3.1`, `vercel ^54.1.0`; ADD `@neondatabase/serverless ^0.10.4`, `drizzle-orm ^0.38.0`; REMOVE `@supabase/supabase-js`
    - devDependencies: KEEP `@vercel/config ^0.5.0`; ADD `typescript ^5.6.0`, `@types/node ^20.16.0`, `@types/react ^18.3.0`, `@types/react-dom ^18.3.0`, `drizzle-kit ^0.29.0`

    Then delete `package-lock.json` (file removal in commit), run `pnpm install` to generate `pnpm-lock.yaml`. Do NOT use `npm install` after this change. Use the `Bash` tool for `rm package-lock.json` and `pnpm install`.

    Note: drizzle-orm subpath `drizzle-orm/neon-http` (used by Plan 02 db.ts) and `drizzle-orm/pg-core` (used by Plan 02 schema.ts) are provided by the `drizzle-orm` package — no extra deps needed.
  </action>
  <verify>
    <automated>test -f pnpm-lock.yaml && ! test -f package-lock.json && grep -q '"packageManager": "pnpm@' package.json && grep -q '"drizzle-orm"' package.json && grep -q '"@neondatabase/serverless"' package.json && grep -q '"typescript"' package.json && ! grep -q '"@supabase/supabase-js"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - File `pnpm-lock.yaml` exists at repo root
    - File `package-lock.json` does NOT exist
    - `package.json` contains the literal substring `"packageManager": "pnpm@`
    - `package.json` contains `"drizzle-orm"`, `"@neondatabase/serverless"`, `"drizzle-kit"`, `"typescript"`, `"@types/node"`, `"@types/react"`, `"@types/react-dom"`
    - `package.json` does NOT contain `"@supabase/supabase-js"`
    - `package.json` scripts contain `db:push` and `typecheck` entries
    - Command `pnpm install` exits 0 (no peer-dep errors that block install)
    - Command `pnpm exec next --version` prints a 15.x version
  </acceptance_criteria>
  <done>pnpm is the active package manager, TS + Drizzle toolchain installed, Supabase client removed, scripts include db:push and typecheck.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add tsconfig.json with strict mode and @/* path alias</name>
  <files>tsconfig.json, next-env.d.ts</files>
  <read_first>
    - .planning/phases/03-foundation/03-PATTERNS.md (section "`tsconfig.json` (config, —)" lines 372-405)
    - .planning/phases/03-foundation/03-CONTEXT.md (D-06 strict true, D-07 path alias @/*)
    - next.config.js (verify no conflicting TS settings)
  </read_first>
  <action>
    Create `tsconfig.json` at repo root with Next.js recommended TypeScript settings:
    - compilerOptions.target: "ES2017"
    - compilerOptions.lib: ["dom", "dom.iterable", "esnext"]
    - compilerOptions.allowJs: true (required during migration — existing .jsx files coexist)
    - compilerOptions.skipLibCheck: true
    - compilerOptions.strict: true (D-06)
    - compilerOptions.noEmit: true
    - compilerOptions.esModuleInterop: true
    - compilerOptions.module: "esnext"
    - compilerOptions.moduleResolution: "bundler"
    - compilerOptions.resolveJsonModule: true
    - compilerOptions.isolatedModules: true
    - compilerOptions.jsx: "preserve"
    - compilerOptions.incremental: true
    - compilerOptions.plugins: [{ "name": "next" }]
    - compilerOptions.paths: { "@/*": ["./src/*"] } (D-07)
    - include: ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"]
    - exclude: ["node_modules"]

    Also create `next-env.d.ts` with the standard Next.js reference comments (Next dev server regenerates this — content is:
    /// <reference types="next" />
    /// <reference types="next/image-types/global" />
    // NOTE: This file should not be edited
    // see https://nextjs.org/docs/app/api-reference/config/typescript for more information.

    Add `next-env.d.ts` to .gitignore is NOT required (Next.js conventions check it in). Leave .gitignore as-is.
  </action>
  <verify>
    <automated>test -f tsconfig.json && test -f next-env.d.ts && grep -q '"strict": true' tsconfig.json && grep -q '"@/\*": \["./src/\*"\]' tsconfig.json && grep -q '"allowJs": true' tsconfig.json && pnpm exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tsconfig.json` exists at repo root
    - File `next-env.d.ts` exists at repo root
    - `tsconfig.json` contains the literal string `"strict": true`
    - `tsconfig.json` contains `"@/*": ["./src/*"]` in paths
    - `tsconfig.json` contains `"allowJs": true`
    - `tsconfig.json` contains `"plugins": [{ "name": "next" }]`
    - Command `pnpm exec tsc --noEmit` exits 0 OR exits non-zero ONLY because of pre-existing .jsx files (no errors originating from tsconfig itself such as TS5023, TS6053, TS18002). If errors come from .jsx files lacking types, document them as expected for Task 3 to resolve for the entry files.
  </acceptance_criteria>
  <done>tsconfig.json drives strict TS compilation with @/* alias. next-env.d.ts present. tsc runs against the project successfully (with allowJs covering legacy .jsx files).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Port root layout, root page, and middleware to TypeScript</name>
  <files>src/app/layout.tsx, src/app/page.tsx, middleware.ts, src/app/layout.jsx, src/app/page.jsx, middleware.js</files>
  <read_first>
    - src/app/layout.jsx (current implementation)
    - src/app/page.jsx (current implementation, note import path `../lib/auth-config.js`)
    - middleware.js (current implementation, note import path `./src/lib/auth-config.js`)
    - src/lib/auth-config.js (verify named export `auth`)
    - src/components/Providers.jsx (verify default export and props)
    - .planning/phases/03-foundation/03-PATTERNS.md (sections `src/app/page.tsx`, `src/app/layout.tsx` lines 153-237)
    - .planning/phases/03-foundation/03-CONTEXT.md (D-07 path alias)
  </read_first>
  <action>
    Port three entry files to TypeScript using `@/` path alias per D-07. Delete the corresponding .jsx/.js files after creating the .ts/.tsx versions (use git rm).

    `src/app/layout.tsx`:
    - Import `type { Metadata } from 'next'`
    - Import `Providers from '@/components/Providers'` (drop the `.jsx` extension — TS resolver handles it via allowJs)
    - Export `const metadata: Metadata = { title: 'We Clínica · Sistema Financeiro' }`
    - Export default `function RootLayout({ children }: { children: React.ReactNode })` returning the same JSX as the existing layout.jsx
    - Preserve EXACTLY: `<html lang="pt-BR">`, the Google Fonts link tag (DM Sans 400/500/600/700, opsz 9..40), preconnect tags (use `crossOrigin="anonymous"` for the gstatic preconnect — JSX strict typing requires a string value), the body style object (margin/padding/fontFamily/background/color/WebkitFontSmoothing)
    - Wrap `{children}` in `<Providers>{children}</Providers>` inside `<body>`

    `src/app/page.tsx`:
    - Import `{ redirect } from 'next/navigation'`
    - Import `{ auth } from '@/lib/auth-config'`
    - Default export `async function Home()` that awaits `auth()`, redirects to `/login` if no session, else redirects to `/dashboard`
    - No explicit return-type annotation needed (redirect returns `never`)

    `middleware.ts`:
    - Import `{ auth } from '@/lib/auth-config'`
    - Default export `auth((req) => { ... })` with the same logic as middleware.js:
      - Destructure `pathname` from `req.nextUrl`
      - `isLoggedIn = !!req.auth`
      - If not logged in and pathname !== '/login' → `Response.redirect(new URL('/login', req.url))`
      - If logged in and pathname === '/login' → `Response.redirect(new URL('/dashboard', req.url))`
    - Export `const config = { matcher: ['/((?!api/auth|_next/static|_next/image|favicon.ico).*)'] }`
    - Type the callback param. Use `import type { NextAuthRequest } from 'next-auth'` if exported by next-auth v5; otherwise accept default inferred type from `auth()` higher-order function (NextAuth v5 infers it). If TS complains, fall back to `(req: any) => ...` with an inline comment `// next-auth v5 beta has no exported request type yet`.

    After creating the three new files, delete the old ones:
    - git rm src/app/layout.jsx
    - git rm src/app/page.jsx
    - git rm middleware.js
  </action>
  <verify>
    <automated>test -f src/app/layout.tsx && test -f src/app/page.tsx && test -f middleware.ts && ! test -f src/app/layout.jsx && ! test -f src/app/page.jsx && ! test -f middleware.js && grep -q "from '@/lib/auth-config'" src/app/page.tsx && grep -q "from '@/lib/auth-config'" middleware.ts && grep -q "from '@/components/Providers'" src/app/layout.tsx && grep -q "Metadata" src/app/layout.tsx && pnpm exec tsc --noEmit src/app/layout.tsx src/app/page.tsx middleware.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Files `src/app/layout.tsx`, `src/app/page.tsx`, `middleware.ts` exist
    - Files `src/app/layout.jsx`, `src/app/page.jsx`, `middleware.js` do NOT exist
    - `src/app/layout.tsx` imports from `@/components/Providers` (uses the `@/` alias)
    - `src/app/page.tsx` imports `{ auth } from '@/lib/auth-config'`
    - `middleware.ts` imports `{ auth } from '@/lib/auth-config'`
    - `middleware.ts` contains the literal matcher string `/((?!api/auth|_next/static|_next/image|favicon.ico).*)`
    - `src/app/layout.tsx` contains `crossOrigin="anonymous"` (the typed strict JSX requirement)
    - `src/app/layout.tsx` contains the literal background color `#F3F2ED` and font family `DM Sans`
    - Running `pnpm exec next build` exits 0 (full build compiles the three entry files plus the rest of the app); if build fails ONLY due to pre-existing .jsx pages in (dashboard) routes that have unrelated TS issues, capture and document them — entry-file compilation MUST succeed
    - Command `pnpm run dev` starts Next.js on port 3000 and `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000` returns one of 200, 302, 307 (redirect to /login or /dashboard is expected behavior, not an error)
  </acceptance_criteria>
  <done>Root layout, root page, and middleware are TypeScript using @/ alias. dev server boots and serves the root route with auth redirect behavior preserved. Old .jsx/.js files removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev environment → package registry | pnpm/npm download paths and package supply chain |
| repo → git history | package-lock.json removal must not leak any embedded credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | pnpm install (`drizzle-orm`, `@neondatabase/serverless`, `drizzle-kit`, `typescript`, `@types/*`) | mitigate | All packages are well-known and recently published by their official publishers (drizzle-team, neondatabase, microsoft, definitelytyped). Pin to explicit minor versions in package.json. Verify pnpm-lock.yaml integrity hashes on install. |
| T-03-02 | Information Disclosure | package-lock.json removal | accept | The lockfile contains no secrets — only public registry URLs and integrity hashes. Removal in git history is safe. |
| T-03-03 | Tampering | tsconfig.json strict false bypass | mitigate | `strict: true` is asserted in acceptance_criteria; CI tsc --noEmit will fail if relaxed. |
| T-03-SC | Tampering | npm package installs (Drizzle + Neon toolchain) | mitigate | All packages are first-party publishers: drizzle-orm and drizzle-kit (drizzle-team org, github.com/drizzle-team/drizzle-orm), @neondatabase/serverless (neondatabase org, github.com/neondatabase/serverless), typescript (microsoft), @types/* (DefinitelyTyped). No `[ASSUMED]` or `[SUS]` packages — all are dominant in their space with millions of weekly downloads. |
</threat_model>

<verification>
- `pnpm run dev` boots Next.js without compile errors
- `curl http://localhost:3000` returns 200, 302, or 307
- `pnpm exec tsc --noEmit` exits 0 on the three new entry files
- `package.json` contains pnpm packageManager field and Drizzle deps
- `pnpm-lock.yaml` exists; `package-lock.json` is gone
</verification>

<success_criteria>
- INFRA-01 partially satisfied (Next.js dev server runs; Drizzle wiring completes in Plan 02)
- Foundation ready for Drizzle schema work in Plan 02
- All three entry files are TypeScript with @/ alias
</success_criteria>

<output>
Create `.planning/phases/03-foundation/03-01-SUMMARY.md` when done. Include:
- Confirmation pnpm-lock.yaml exists and package-lock.json removed
- Output of `pnpm exec tsc --noEmit` (full or summarized)
- Output of `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000` against the running dev server
- pnpm version used and the pinned drizzle-orm/@neondatabase/serverless versions resolved in the lockfile
- Any .jsx files in the project that still produce TS warnings under allowJs (documented for Phase 6 cleanup)
</output>
