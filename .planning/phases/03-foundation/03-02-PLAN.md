---
phase: 03-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/db/schema.ts
  - src/lib/db.ts
  - drizzle.config.ts
  - .env.example
  - .env.local
autonomous: true
requirements:
  - INFRA-03
  - DB-01
must_haves:
  truths:
    - "Drizzle schema defines all 8 tables: usuarios_permitidos, clientes, prestadores, plano_contas, premissas, lancamentos, audit_log (D-09)"
    - "Column renames from D-08 are applied: comissao_pct, categoria, observacao, data_pagamento_sexta"
    - "`pnpm exec tsc --noEmit src/db/schema.ts src/lib/db.ts` exits 0"
    - "Drizzle config points to schema.ts and uses DATABASE_URL"
    - "`.env.example` documents DATABASE_URL"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Drizzle pgTable definitions for 8 tables with type inference exports"
      contains: "pgTable"
    - path: "src/lib/db.ts"
      provides: "Neon serverless + Drizzle client singleton"
      contains: "neon"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit configuration"
      contains: "defineConfig"
    - path: ".env.example"
      provides: "Documented env vars including DATABASE_URL"
      contains: "DATABASE_URL"
  key_links:
    - from: "src/lib/db.ts"
      to: "src/db/schema.ts"
      via: "import schema for relational queries"
      pattern: "from '@/db/schema'"
    - from: "drizzle.config.ts"
      to: "src/db/schema.ts"
      via: "schema path"
      pattern: "./src/db/schema.ts"
---

<objective>
Define the Drizzle ORM schema for all 8 business tables and wire the Drizzle client to Neon serverless, plus configure drizzle-kit for migration commands.

Purpose: This plan delivers the type-safe data layer (DB-01) and the ORM wiring (INFRA-03) that subsequent phases consume. Without schema.ts, Plan 04's drizzle-kit push has nothing to apply. Without db.ts, Phase 6 server actions have no client to call.

Output: Three TypeScript artifacts (schema, client, drizzle config) and updated env documentation. No database connection is required at this stage — drizzle-kit only needs DATABASE_URL at push time (Plan 04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-foundation/03-CONTEXT.md
@.planning/phases/03-foundation/03-PATTERNS.md
@.planning/phases/03-foundation/03-01-SUMMARY.md

@supabase/schema.sql
@src/lib/supabase.js
@.env.local
@.env.example

<interfaces>
Drizzle v0.38 + Neon HTTP driver — key APIs the executor will use:

From drizzle-orm/pg-core:
  - pgTable(name, columnsObject, optionalCallback)
  - uuid('col').primaryKey().defaultRandom()
  - text('col').notNull()
  - boolean('col').notNull().default(true)
  - numeric('col', { precision?, scale? }).notNull().default('0')
  - date('col')
  - timestamp('col', { withTimezone: true }).defaultNow()
  - index('idx_name').on(table.column)
  - check('constraint_name', sql`<expression>`)  // for CHECK constraints
  - references(() => otherTable.id, { onDelete: 'cascade' })

From drizzle-orm:
  - sql template tag for raw SQL fragments

From drizzle-orm/neon-http:
  - drizzle(neonClient, { schema })

From @neondatabase/serverless:
  - neon(connectionString)

From drizzle-kit:
  - defineConfig({ schema, out, dialect, dbCredentials })

Source-of-truth column types (from supabase/schema.sql, lines 12-117):
  usuarios_permitidos: id uuid pk, email text unique not null, nome text not null, role text default 'operacional' check(admin|operacional), ativo bool default true, created_at timestamptz
  clientes: id uuid pk, nome text not null, responsavel text, telefone text, email text, obs text, created_at timestamptz
  prestadores: id uuid pk, nome text not null, tipo text check(psicologo|at), com numeric default 0, obs text, ativo bool default true, created_at timestamptz
  plano_contas: id uuid pk, cod text not null, nome text not null, tipo text check(receita|despesa), cat text, created_at timestamptz
  premissas: chave text pk, valor numeric default 0, updated_at timestamptz
  lancamentos: id uuid pk, tipo text check(receita|despesa), descricao text not null, valor numeric not null, data date not null, data_competencia date, data_vencimento date, data_pagamento date, conta_id uuid FK→plano_contas, cliente_id uuid FK→clientes, prestador_id uuid FK→prestadores, forma_pagamento text, status text default 'pendente' check(pendente|recebido|pago|cancelado|projecao), horas numeric default 0, obs text, auto_comissao bool default false, ref_lanc_id uuid FK→lancamentos on delete cascade, sexta date, conciliado bool default false, created_by text, created_at timestamptz, updated_at timestamptz
  audit_log: id uuid pk, user_email text not null, user_nome text, acao text not null, entidade text not null, detalhes text, created_at timestamptz

Indexes (supabase/schema.sql lines 97-102, 117):
  idx_lancamentos_data, idx_lancamentos_tipo, idx_lancamentos_status, idx_lancamentos_prestador, idx_lancamentos_ref, idx_lancamentos_sexta (on data_pagamento_sexta after rename), idx_audit_created (on created_at desc)

D-08 rename map (applies to column name + TS field name; database column name uses the snake_case rename):
  prestadores.com           → comissao_pct        (numeric, default 0)
  plano_contas.cat          → categoria           (text)
  clientes.obs              → observacao          (text)
  prestadores.obs           → observacao          (text)
  lancamentos.obs           → observacao          (text)
  lancamentos.sexta         → data_pagamento_sexta (date)

D-09 tables kept (8 total): usuarios_permitidos, clientes, prestadores, plano_contas, premissas, lancamentos, audit_log. (NextAuth tables = Phase 4 responsibility.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Drizzle schema with 8 tables and D-08 renames</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - supabase/schema.sql (lines 12-117 for all 8 tables, lines 97-102 for lancamentos indexes, line 117 for audit_log index)
    - .planning/phases/03-foundation/03-PATTERNS.md (section "`src/db/schema.ts`" lines 25-82 for Drizzle pattern, column rename map)
    - .planning/phases/03-foundation/03-CONTEXT.md (D-08 renames, D-09 8 tables)
  </read_first>
  <action>
    Create `src/db/schema.ts` translating supabase/schema.sql to Drizzle `pgTable` calls. Apply D-08 renames at the database-column-name level (the rename map is in the interfaces block above).

    Import block at top:
    - `import { pgTable, uuid, text, boolean, numeric, date, timestamp, index, check } from 'drizzle-orm/pg-core'`
    - `import { sql } from 'drizzle-orm'`

    Export each table as a const using camelCase TypeScript identifier and snake_case database name:
    - `usuariosPermitidos` (table `usuarios_permitidos`) — columns: id uuid pk defaultRandom, email text unique notNull, nome text notNull, role text notNull default 'operacional' with check `role IN ('admin','operacional')`, ativo boolean notNull default true, createdAt timestamp withTimezone defaultNow
    - `clientes` (table `clientes`) — columns: id uuid pk defaultRandom, nome text notNull, responsavel text, telefone text, email text, observacao text (db column `observacao`, was `obs`), createdAt timestamp withTimezone defaultNow
    - `prestadores` (table `prestadores`) — columns: id uuid pk defaultRandom, nome text notNull, tipo text notNull with check `tipo IN ('psicologo','at')`, comissaoPct numeric notNull default '0' (db column `comissao_pct`, was `com`), observacao text (db column `observacao`, was `obs`), ativo boolean notNull default true, createdAt timestamp withTimezone defaultNow
    - `planoContas` (table `plano_contas`) — columns: id uuid pk defaultRandom, cod text notNull, nome text notNull, tipo text notNull with check `tipo IN ('receita','despesa')`, categoria text (db column `categoria`, was `cat`), createdAt timestamp withTimezone defaultNow
    - `premissas` (table `premissas`) — columns: chave text primaryKey, valor numeric notNull default '0', updatedAt timestamp withTimezone defaultNow
    - `lancamentos` (table `lancamentos`) — columns: id uuid pk defaultRandom, tipo text notNull with check `tipo IN ('receita','despesa')`, descricao text notNull, valor numeric notNull, data date notNull, dataCompetencia date (`data_competencia`), dataVencimento date (`data_vencimento`), dataPagamento date (`data_pagamento`), contaId uuid references planoContas.id (`conta_id`), clienteId uuid references clientes.id (`cliente_id`), prestadorId uuid references prestadores.id (`prestador_id`), formaPagamento text (`forma_pagamento`), status text notNull default 'pendente' with check `status IN ('pendente','recebido','pago','cancelado','projecao')`, horas numeric default '0', observacao text (db column `observacao`, was `obs`), autoComissao boolean notNull default false (`auto_comissao`), refLancId uuid references (() => lancamentos.id, { onDelete: 'cascade' }) — self-reference (`ref_lanc_id`), dataPagamentoSexta date (db column `data_pagamento_sexta`, was `sexta`), conciliado boolean notNull default false, createdBy text (`created_by`), createdAt timestamp withTimezone defaultNow, updatedAt timestamp withTimezone defaultNow
    - `auditLog` (table `audit_log`) — columns: id uuid pk defaultRandom, userEmail text notNull (`user_email`), userNome text (`user_nome`), acao text notNull, entidade text notNull, detalhes text, createdAt timestamp withTimezone defaultNow

    For self-referencing FK on `lancamentos.refLancId`, use the deferred reference callback form: `uuid('ref_lanc_id').references((): any => lancamentos.id, { onDelete: 'cascade' })` — Drizzle requires the `(): any =>` workaround for circular type inference.

    Add indexes for `lancamentos` and `audit_log` using the 3-argument pgTable form:
    ```
    export const lancamentos = pgTable('lancamentos', { /* columns */ }, (table) => ({
      idxData: index('idx_lancamentos_data').on(table.data),
      idxTipo: index('idx_lancamentos_tipo').on(table.tipo),
      idxStatus: index('idx_lancamentos_status').on(table.status),
      idxPrestador: index('idx_lancamentos_prestador').on(table.prestadorId),
      idxRef: index('idx_lancamentos_ref').on(table.refLancId),
      idxSexta: index('idx_lancamentos_sexta').on(table.dataPagamentoSexta),
    }))
    export const auditLog = pgTable('audit_log', { /* columns */ }, (table) => ({
      idxCreated: index('idx_audit_created').on(table.createdAt),
    }))
    ```

    After all tables, export type aliases for every table (used by future server actions and the seed file in Plan 04):
    ```
    export type UsuarioPermitido = typeof usuariosPermitidos.$inferSelect
    export type NewUsuarioPermitido = typeof usuariosPermitidos.$inferInsert
    // repeat for each table
    ```

    NOT in this file: NextAuth tables (D-09, Phase 4), seed data (Plan 04 owns seed migration).
  </action>
  <verify>
    <automated>test -f src/db/schema.ts && grep -c "pgTable" src/db/schema.ts | awk '$1 >= 7 {exit 0} {exit 1}' && grep -q "usuariosPermitidos" src/db/schema.ts && grep -q "comissao_pct" src/db/schema.ts && grep -q "data_pagamento_sexta" src/db/schema.ts && grep -q "'observacao'" src/db/schema.ts && grep -q "'categoria'" src/db/schema.ts && grep -q "auditLog" src/db/schema.ts && grep -q "\$inferSelect" src/db/schema.ts && pnpm exec tsc --noEmit src/db/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/db/schema.ts` exists
    - File contains at least 7 `pgTable(` calls (one per table: usuariosPermitidos, clientes, prestadores, planoContas, premissas, lancamentos, auditLog)
    - File contains the literal database column strings `'comissao_pct'`, `'data_pagamento_sexta'`, `'observacao'`, `'categoria'` (D-08 renames applied)
    - File does NOT contain the legacy column names as string literals: `'com'` (as standalone column), `'sexta'`, `'cat'`, `'obs'` — `grep -E "'(com|sexta|cat|obs)'" src/db/schema.ts` returns no matches
    - File contains check constraints for role, tipo (prestadores), tipo (plano_contas), tipo (lancamentos), status — at least 5 `check(` calls
    - File contains 7 `$inferSelect` exports (one per table)
    - File contains a self-reference for lancamentos.refLancId (`ref_lanc_id` with `onDelete: 'cascade'`)
    - File contains 6 indexes on `lancamentos` plus 1 on `auditLog` (grep `index('idx_` returns at least 7 matches)
    - `pnpm exec tsc --noEmit src/db/schema.ts` exits 0
  </acceptance_criteria>
  <done>Schema TypeScript file defines all 8 tables with D-08 renames, indexes, check constraints, self-reference, FK references, and inferred type exports. tsc validates the file in strict mode.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Drizzle client singleton and drizzle-kit config</name>
  <files>src/lib/db.ts, drizzle.config.ts</files>
  <read_first>
    - src/lib/supabase.js (current singleton pattern + env-guard convention, lines 1-15)
    - src/db/schema.ts (verify the file exists from Task 1, confirm exports)
    - .planning/phases/03-foundation/03-PATTERNS.md (sections "`src/lib/db.ts`" lines 86-125 and "`drizzle.config.ts`" lines 129-150)
    - .planning/phases/03-foundation/03-CONTEXT.md (D-07 path alias, D-12 Neon Marketplace injects DATABASE_URL)
  </read_first>
  <action>
    Create `src/lib/db.ts` matching the singleton pattern in `src/lib/supabase.js`:
    - Import `{ neon } from '@neondatabase/serverless'`
    - Import `{ drizzle } from 'drizzle-orm/neon-http'`
    - Import `* as schema from '@/db/schema'`
    - Read `const databaseUrl = process.env.DATABASE_URL`
    - If `!databaseUrl`, log warning (do not throw — match the project convention from src/lib/supabase.js):
      `// eslint-disable-next-line no-console`
      `console.warn('[db] DATABASE_URL ausente em .env')`
    - Export `const sql = neon(databaseUrl ?? 'postgres://placeholder')` — placeholder so import-time evaluation does not crash if env missing
    - Export `const db = drizzle(sql, { schema })`
    - Export `export type Database = typeof db` for downstream typing

    Create `drizzle.config.ts` at repo root:
    - Import `{ defineConfig } from 'drizzle-kit'`
    - Default export `defineConfig({ schema: './src/db/schema.ts', out: './drizzle', dialect: 'postgresql', dbCredentials: { url: process.env.DATABASE_URL! } })`
    - Add a top-of-file comment: `// drizzle-kit reads DATABASE_URL from .env.local for local dev; Vercel Marketplace injects it in prod (D-12).`

    Note: drizzle-kit by default loads `.env` and `.env.local` via dotenv-style resolution. No explicit dotenv import needed.
  </action>
  <verify>
    <automated>test -f src/lib/db.ts && test -f drizzle.config.ts && grep -q "from '@neondatabase/serverless'" src/lib/db.ts && grep -q "from 'drizzle-orm/neon-http'" src/lib/db.ts && grep -q "from '@/db/schema'" src/lib/db.ts && grep -q "export const db" src/lib/db.ts && grep -q "defineConfig" drizzle.config.ts && grep -q "'./src/db/schema.ts'" drizzle.config.ts && grep -q "'postgresql'" drizzle.config.ts && pnpm exec tsc --noEmit src/lib/db.ts drizzle.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/db.ts` exists
    - File `drizzle.config.ts` exists at repo root
    - `src/lib/db.ts` imports `neon` from `@neondatabase/serverless`
    - `src/lib/db.ts` imports `drizzle` from `drizzle-orm/neon-http`
    - `src/lib/db.ts` imports schema using the `@/` alias (`from '@/db/schema'`)
    - `src/lib/db.ts` exports a `db` const (literal substring `export const db`)
    - `src/lib/db.ts` contains the env-guard string `[db] DATABASE_URL ausente em .env`
    - `drizzle.config.ts` calls `defineConfig` with `dialect: 'postgresql'` and `schema: './src/db/schema.ts'`
    - `drizzle.config.ts` references `process.env.DATABASE_URL`
    - `pnpm exec tsc --noEmit src/lib/db.ts drizzle.config.ts` exits 0
    - Command `pnpm exec drizzle-kit --version` exits 0 (drizzle-kit binary is reachable via pnpm)
  </acceptance_criteria>
  <done>db.ts singleton wires Neon serverless + Drizzle with schema, env-guard matches project convention. drizzle.config.ts is ready for drizzle-kit push in Plan 04. Both files type-check.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update .env.example and .env.local to document Neon DATABASE_URL</name>
  <files>.env.example, .env.local</files>
  <read_first>
    - .env.example (current state)
    - .env.local (current state — verify it is gitignored and ONLY edit local placeholder values, do NOT touch real secrets)
    - .planning/phases/03-foundation/03-PATTERNS.md (section "`.env.local`" lines 268-304)
    - .planning/phases/03-foundation/03-CONTEXT.md (D-12 Vercel Marketplace injects DATABASE_URL)
  </read_first>
  <action>
    Update `.env.example` (committed to repo, NO real secrets) with:
    ```
    # Neon PostgreSQL connection (provisioned via Vercel Marketplace integration — D-12)
    # Local dev: paste from Neon dashboard → Connection Details
    # Production: auto-injected by Vercel
    DATABASE_URL=postgres://user:pass@host.neon.tech/dbname?sslmode=require

    # NextAuth.js v5 (configured in Phase 4 — present here for reference)
    AUTH_SECRET=
    AUTH_URL=http://localhost:3000
    AUTH_GOOGLE_ID=
    AUTH_GOOGLE_SECRET=
    ```

    Remove from `.env.example` any legacy `VITE_SUPABASE_*`, `SUPABASE_*`, `NEXT_PUBLIC_SUPABASE_*` entries.

    For `.env.local` (gitignored, contains real values): ADD a `DATABASE_URL=` line with a placeholder value `postgres://placeholder_replace_in_plan_03_after_neon_provision` so dev server boots without DB queries hitting real Neon (Plan 03 / user will replace with the real value from Vercel/Neon dashboard). Do NOT remove or modify existing `AUTH_SECRET`, `AUTH_URL`, `AUTH_GOOGLE_ID`, `AUTH_GOOGLE_SECRET` values. Do NOT remove existing Supabase vars from .env.local in this task — they are leftovers from v1.0 but removing them is out of scope for this plan (Plan 04 cleanup task can remove them post-push).
  </action>
  <verify>
    <automated>test -f .env.example && test -f .env.local && grep -q "^DATABASE_URL=" .env.example && grep -q "DATABASE_URL=postgres://user:pass" .env.example && grep -q "^AUTH_SECRET=" .env.example && ! grep -qE "^(VITE_SUPABASE|NEXT_PUBLIC_SUPABASE|SUPABASE_)" .env.example && grep -q "^DATABASE_URL=" .env.local</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` exists and contains a line starting with `DATABASE_URL=`
    - `.env.example` shows the postgres URL format `postgres://user:pass@host.neon.tech/dbname?sslmode=require`
    - `.env.example` contains `AUTH_SECRET=`, `AUTH_URL=`, `AUTH_GOOGLE_ID=`, `AUTH_GOOGLE_SECRET=` (Phase 4 reference)
    - `.env.example` does NOT contain any line matching `^VITE_SUPABASE`, `^NEXT_PUBLIC_SUPABASE`, or `^SUPABASE_`
    - `.env.local` contains a line starting with `DATABASE_URL=` (placeholder value acceptable at this stage)
    - `.env.local` still contains the original `AUTH_*` values (executor must verify they are NOT empty / NOT changed — diff before/after should only add lines, not remove auth values)
  </acceptance_criteria>
  <done>.env.example documents DATABASE_URL and AUTH_* vars for Phase 4. Supabase legacy env vars removed from .env.example. .env.local has a DATABASE_URL placeholder; real value comes in Plan 03 after Neon provisioning. AUTH_* values preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.local → developer machine | DATABASE_URL placeholder must not be committed (file is gitignored — verify) |
| .env.example → public repo | example file is committed; MUST NOT contain real connection strings |
| application → Neon | client-side code should never import db.ts (server-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | .env.example committed with real DATABASE_URL | mitigate | Acceptance criteria asserts placeholder URL format (`postgres://user:pass@host.neon.tech/dbname`). Reviewer must verify before commit. |
| T-03-05 | Information Disclosure | .env.local accidentally committed | mitigate | `.gitignore` already excludes `.env*` patterns (verified in Phase 1). Task does not modify .gitignore. |
| T-03-06 | Tampering | src/db/schema.ts column rename inconsistency | mitigate | Acceptance criteria greps for renamed db column strings AND asserts legacy strings (`'com'`, `'sexta'`, `'cat'`, `'obs'`) are absent. |
| T-03-07 | Information Disclosure | client-side import of src/lib/db.ts | accept | Phase 3 doesn't add any consumer of db.ts. Phase 6 will add server actions with `'use server'` directive. Phase 4 NextAuth callbacks run server-side. |
| T-03-08 | Tampering | drizzle-kit reads DATABASE_URL from wrong .env file | mitigate | drizzle-kit reads .env.local by default in dev. Plan 04 verifies push hits the right Neon DB before applying. |
</threat_model>

<verification>
- All three artifacts exist (schema.ts, db.ts, drizzle.config.ts)
- `pnpm exec tsc --noEmit src/db/schema.ts src/lib/db.ts drizzle.config.ts` exits 0
- `.env.example` documents DATABASE_URL with placeholder
- `pnpm exec drizzle-kit --help` exits 0 (CLI works)
- Plan 03 (Neon provisioning checkpoint) can now run, then Plan 04 can push the schema
</verification>

<success_criteria>
- INFRA-03 satisfied: Drizzle ORM configured with Neon serverless driver
- DB-01 satisfied: schema defines all 8 tables with D-08 renames applied
- Plan 04 can run `pnpm exec drizzle-kit push` once Plan 03 provisions DATABASE_URL
</success_criteria>

<output>
Create `.planning/phases/03-foundation/03-02-SUMMARY.md` when done. Include:
- Output of `pnpm exec tsc --noEmit src/db/schema.ts src/lib/db.ts drizzle.config.ts`
- Final list of 7 pgTable identifiers in schema.ts (grep result)
- Confirmation that D-08 renames are applied (grep result for `comissao_pct|data_pagamento_sexta|observacao|categoria`)
- Confirmation that legacy column names are absent
- The exact drizzle-kit and drizzle-orm versions resolved in pnpm-lock.yaml
</output>
