# Dev local: rode `vercel env pull .env.local` para baixar do Vercel,
# OU preencha estes manualmente se não usar a integration Vercel↔Supabase.
#
# A integration injeta SUPABASE_URL e SUPABASE_ANON_KEY (sem prefixo VITE_).
# O vite.config.js aceita ambos os formatos.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
